name: "PR Checks Workflow"

on: pull_request

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Run dummy step
        run: echo "This is a dummy step."
  compile-c-code:
    if: ${{ false }}
    uses: ./.github/workflows/c-cpp.yml
  # report-status:
  #   runs-on: ubuntu-latest
  #   needs: [compile-c-code]
  #   if: ${{ always() }}
  #   steps:
  #     - name: Report status
  #       run: |
  #         # Retrieve the actual result of the reusable workflow job
  #         RESULT="${{ needs.compile-c-code.result }}"
          
  #         echo "The reusable workflow result is: $RESULT"
          
  #         # Allow both explicitly successful and skipped runs to pass the PR
  #         if [ "$RESULT" = "success" ] || [ "$RESULT" = "skipped" ]; then
  #           echo "CI requirements met safely."
  #           exit 0
  #         else
  #           echo "CI failed or was cancelled."
  #           exit 1
  #         fi
